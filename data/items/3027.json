{
  "passives": [
    {
      "unique": true,
      "code": [
        "return function(champion, slot){",
        "champion.add.mana += calc.getItemStacks(slot) * 40",
        "champion.add.health += calc.getItemStacks(slot) * 20",
        "champion.add.ap += calc.getItemStacks(slot) * 4",
        "}"
      ],
      "name": "roapass"
    }
  ],
  "stacks": 10
}